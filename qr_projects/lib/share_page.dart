import 'package:flutter/material.dart';

class SharePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Paylaşım Yapmak'),
      ),
      body: Center(
        child: Text('Paylaşım Yapma QR Kodu Oluşturma Sayfası'),
      ),
    );
  }
}
