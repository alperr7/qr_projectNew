import 'package:flutter/material.dart';

class PlateReaderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plaka Okuma'),
      ),
      body: Center(
        child: Text('Plaka Okuma Page'),
      ),
    );
  }
}
